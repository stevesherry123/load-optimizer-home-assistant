# Legacy

This folder is reserved for the original appliance-specific source material during migration.

The goal is to replace legacy helper names, scripts, and dashboards with the `load_optimizer_*` schema and then delete the old files once the migration is complete.

