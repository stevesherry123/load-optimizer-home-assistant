# Home Assistant Layout

This folder contains the Home Assistant-facing implementation for Load Optimizer.

Recommended subfolders:

- `helpers/` for helper entity definitions
- `templates/` for template sensors
- `pyscript/` for the shared learning and optimisation logic
- `dashboards/` for Lovelace card definitions
- `legacy/` for source material being retired during migration

